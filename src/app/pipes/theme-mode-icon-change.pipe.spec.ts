import { ThemeModeIconChangePipe } from './theme-mode-icon-change.pipe';

describe('ThemeModeIconChangePipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeModeIconChangePipe();
    expect(pipe).toBeTruthy();
  });
});
